# Cloudflare Pages _redirects config
# Make /example/* return 404 (used as reference template only, not for public)

/example/*   /404   404

# 2026-07-06 fix: archive 路径 308 循环 + SPA fallback
# Cloudflare Pages 默认 trailing-slash redirect 把 /archive → /archive → ... 循环
# 强制重定向到 .html 物理文件,跳出循环
/archive      /archive.html   308
/archive/     /archive.html   308