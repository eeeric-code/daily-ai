# Cloudflare Pages _redirects config
# Make /example/* return 404 (used as reference template only, not for public)

/example/*   /404   404

# 2026-07-06 fix: archive 路径 308 无限循环 + SPA fallback → portal 首页
# 用 200 rewrite 直接 serve archive.html 内容,绕过 308 + fallback
/archive      /archive.html  200
/archive/     /archive.html  200
/archive.html /archive.html  200